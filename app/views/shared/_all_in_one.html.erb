
<div class="hero-unit">
  
  
    <% if @page.posts.count > 0 %>
      
      <ol class="microposts">
        <%= render :partial => @page.posts %>
      </ol>
   
     <% else %>
     На цій сторінці немає повидомлень
  <% end %>
  </div>
<% if signed_in? %>
<%= render 'posts/form' %>
<% end %>