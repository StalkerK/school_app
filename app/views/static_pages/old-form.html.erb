
<%= form_for Post.new do |f| %>
  <% if Post.new.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@Post.new.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% Post.new.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<% if params[:page] %><input type="hidden" id="post_page_id" name="post[page_id]" value="<%= params[:page] %>" />
<% end %>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  
  </div>
    <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
