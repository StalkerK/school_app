<div class="hero-unit">
  
<h3>Результати пошуку</h3>
   
    <div class="microposts"> 
    <% @posts.each do |post| %>
       
       <%= raw post.body %>
       <hr>
    <% end %>
<%= link_to 'Головна сторінка', root_path %>
</div>
</div>