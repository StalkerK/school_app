<div class="hero-unit">
<%= form_for([@page, @page.posts.build], :html => { :multipart => true }) do |f| %>

  <% if @page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label 'Текст повідомлення' %><br>
    <%= f.text_area :body %>
  </div>

  <div class="actions">
  <div class="field">
    <%= f.file_field :photo %>
  </div>
  <div class="actions">

    <%= f.submit "Cтворити повідомлення" %>
  </div>
<% end %>
</div>
</div>